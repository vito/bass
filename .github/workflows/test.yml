name: test

on:
  push:
    branches: ["*"]
  pull_request:
    branches: ["*"]

jobs:
  bass:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: actions/setup-go@v2
      with: {go-version: 1.17}
    - run: ./ci/actions-test

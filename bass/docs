#!/usr/bin/env bass

(defn main []
  (for [{src (:src *dir*/../)
         version (:version "dev")
         os (:os "linux")
         arch (:arch "amd64")} *stdin*]
    (use (src/project))
    (emit (project:docs src) *stdout*)))

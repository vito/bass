#!/usr/bin/env bass

(use (*dir*/../project)
     (*dir*/bass))

(def [ref & testflags]
  *args*)

(let [src (bass:checkout ref)]
  (log "running tests")
  ((project:tests src ["./..."]))

  (log "running nix checks")
  ((project:nix-checks src)))

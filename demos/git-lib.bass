(defn git-clone [uri]
  (-> (from "alpine/git"
        ($ git clone $uri ./))
      (path ./)))

(def bass-src
  (git-clone "https://github.com/vito/bass"))

(import (load (bass-src/demos/lib/images))
        bass)

(run
  (from (bass bass-src)
    ($ bass --version)))

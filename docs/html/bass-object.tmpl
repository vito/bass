<div class="delimited">
  <div class="open"><code class="chroma p">{</code></div>
  <div class="content">
    <div class="object">
      {{range .Content}}
      <div class="object-field">
        {{range .}}
        <div class="object-cell">{{. | render}}</div>
        {{end}}
      </div>
      {{end}}
    </div>
  </div>
  <div class="close"><code class="chroma p">}</code></div>
</div>

(def create-files
  "echo 42 > foo; mkdir some-dir; echo '\"hello\"' > some-dir/some-file")

(def hello
  (let [create (-> ($ .sh -c $create-files)
                   alpine-test)]
    (path create ./)))

(export hello ./all/)
(export hello/some-dir/some-file ./just-some-file/)

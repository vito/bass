(defn in-alpine [workload]
  (in-image workload {:repository "alpine"}))

(defn on-test-platform [workload]
  (on-platform workload {:test true}))

(defn alpine-test [workload]
  (-> workload in-alpine on-test-platform))

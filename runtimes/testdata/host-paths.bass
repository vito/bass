(defn cat [path]
  (-> ($ cat $path)
      (response-from :stdout)))

[(next
   (run
     (from "alpine"
       (cat *dir*/host-paths/number))))
 (next
   (run
     (from "alpine"
       (cd *dir*/host-paths/
         (cat ./number)))))]

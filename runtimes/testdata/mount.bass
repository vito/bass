(def created
  (path
    (from (linux/alpine)
      ($ mkdir ./some-dir/)
      ($ sh -c "echo 42 > some-dir/some-file"))
    ./some-dir/))

(-> ($ cat ./foo/some-file)
    (in-image (linux/alpine))
    (with-mount created/ ./foo/)
    (read :json)
    next)

(def created
  (path (-> ($ sh -c "echo 42 -> hello")
            (in-image (linux/alpine)))
        ./hello))

(next (run (-> ($ cat $created)
               (in-image (linux/alpine))
               (response-from :stdout))))

(def create-script
  "echo \"#!/bin/sh\n\necho 42\n\" > some-script; chmod +x some-script")

(def hello
  (let [create (-> ($ sh -c $create-script)
                   (in-image (linux/alpine)))]
    (path create ./)))

(next ((-> (hello/some-script)
           (in-image (linux/alpine))
           (response-from :stdout))))

(def hello
  (path
    (from (linux/alpine)
      ($ sh -c "echo \"#!/bin/sh\n\necho 42\n\" > some-script")
      ($ chmod +x ./some-script))
    ./))

(next (read (-> (hello/some-script)
               (in-image (linux/alpine)))
            :json))
